name: first-workflow
run-name: First workflow

on:
  workflow_dispatch:

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v4

      - name: Step1
        run: echo "step1"

      - name: Step2
        run: echo "step2"